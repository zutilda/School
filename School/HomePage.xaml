<Page x:Class="School.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:School"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="HomePage">

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Horizontal">
            <Button Name="Services" Template="{StaticResource Button}" Click="Services_Click">Услуги</Button>
            <Button Name="Admin" Template="{StaticResource Button}" Click="Admin_Click">Админ</Button>
            <Button Name="Note" Template="{StaticResource Button}" Visibility="Collapsed" Click="Note_Click">Ближайшие записи</Button>
        </StackPanel>
    </Grid>
</Page>
